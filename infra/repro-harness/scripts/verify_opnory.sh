#!/usr/bin/env bash
# Phase 1B placeholder: Opnory application verification against the lab.
# Phase 1A does NOT define application-level verification; the verifier worker
# and Phase 1B contract will pin the exact checks (API healthz, auth boundary
# probe, governance smoke test). This script fails closed until replaced.
echo "verify_opnory.sh: Phase 1B placeholder — no application verification defined yet" >&2
exit 3
